import { Component } from '@angular/core';

@Component({
  // The name that will be used in the HTML to call this component
  selector: 'ManualComponent',
  //Link to the HTML file that will be used to display the component
  templateUrl: './manual.component.html',
})
export class ManualComponent {}
